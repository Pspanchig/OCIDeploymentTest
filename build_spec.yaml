version: 0.1
component: build
timeoutInSeconds: 1200
shell: bash
steps:
  - type: Command
    name: Build image
    command: |
      docker build -t $IMAGE_NAME .
      docker push  $IMAGE_NAME
outputArtifacts:
  - name: react-image
    type: DOCKER_IMAGE
    location: $IMAGE_NAME
